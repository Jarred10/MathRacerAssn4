<h1>All Users</h1>

	<%# Print all users usernames as a link to their page and thier total wins %>
    <% @users.each do |user| %>
      <h3><p><%= link_to user.username, user %></p></h3>
       <p>Wins: <%= user.wins %> </br></p>
    <% end %>
	
	<%# Link to the previous page%>
	<%= link_to 'Back', root_url %>
